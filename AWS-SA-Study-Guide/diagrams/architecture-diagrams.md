# Architecture Diagrams

ASCII reference architectures for common AWS SAA-C03 exam patterns.

---

## 1. Three-Tier Web Application

```
                            ┌──────────────┐
                            │   Route 53   │
                            │   (DNS)      │
                            └──────┬───────┘
                                   │
                            ┌──────┴───────┐
                            │  CloudFront  │
                            │   (CDN)      │
                            └──────┬───────┘
                                   │
                ┌──────────────────┴──────────────────┐
                │              VPC (10.0.0.0/16)      │
                │                                      │
                │  ┌─── Public Subnets ──────────────┐│
                │  │                                  ││
                │  │  ┌──────────┐    ┌──────────┐   ││
                │  │  │   ALB    │    │   ALB    │   ││
                │  │  │  (AZ-a)  │    │  (AZ-b)  │   ││
                │  │  └────┬─────┘    └────┬─────┘   ││
                │  │       │               │          ││
                │  └───────┼───────────────┼──────────┘│
                │          │               │           │
                │  ┌─── Private Subnets (App) ───────┐│
                │  │       │               │          ││
                │  │  ┌────┴─────┐    ┌────┴─────┐   ││
                │  │  │EC2 (ASG) │    │EC2 (ASG) │   ││
                │  │  │  (AZ-a)  │    │  (AZ-b)  │   ││
                │  │  └────┬─────┘    └────┬─────┘   ││
                │  │       │               │          ││
                │  └───────┼───────────────┼──────────┘│
                │          │               │           │
                │  ┌─── Private Subnets (DB) ────────┐│
                │  │       │               │          ││
                │  │  ┌────┴─────┐    ┌────┴─────┐   ││
                │  │  │   RDS    │◄──►│   RDS    │   ││
                │  │  │ Primary  │sync│ Standby  │   ││
                │  │  │  (AZ-a)  │    │  (AZ-b)  │   ││
                │  │  └──────────┘    └──────────┘   ││
                │  │          Multi-AZ                ││
                │  └──────────────────────────────────┘│
                │                                      │
                └──────────────────────────────────────┘
```

**Key points:**
- ALB in public subnets (internet-facing)
- App servers in private subnets (ASG for auto scaling)
- Database in private subnets (Multi-AZ for HA)
- NAT Gateway in public subnets for outbound from private subnets

---

## 2. Serverless API Architecture

```
                      ┌───────────────┐
                      │   Cognito     │
                      │  User Pool    │
                      │  (Auth)       │
                      └───────┬───────┘
                              │ JWT Token
                              │
┌──────────┐           ┌──────┴───────┐          ┌──────────────┐
│  Client  │──HTTPS──►│ API Gateway  │────────►│   Lambda     │
│ (Mobile/ │           │  (REST API)  │          │  Functions   │
│  Web)    │           └──────────────┘          └──────┬───────┘
└──────────┘                                           │
      │                                      ┌─────────┼─────────┐
      │                                      │         │         │
      │                                 ┌────┴───┐ ┌───┴───┐ ┌──┴──────┐
      │                                 │DynamoDB│ │  S3   │ │   SES   │
      │                                 │(Table) │ │(Files)│ │ (Email) │
      │                                 └────────┘ └───────┘ └─────────┘
      │
      │          ┌──────────────┐           ┌──────────────┐
      └─────────►│  CloudFront  │──────────►│   S3 Bucket  │
                 │  (CDN)       │           │ (Static Site) │
                 └──────────────┘           └──────────────┘
```

**Key points:**
- API Gateway provides RESTful interface + throttling + caching
- Cognito handles authentication (JWT tokens)
- Lambda handles business logic (pay-per-request)
- DynamoDB for data persistence (serverless, auto-scaling)
- CloudFront + S3 for static website hosting (SPA)
- Fully serverless — no servers to manage

---

## 3. Multi-Region Active-Passive DR

```
                        ┌──────────────────┐
                        │    Route 53      │
                        │ Failover Routing │
                        └───────┬──────────┘
                     ┌──────────┼──────────────┐
                     │ Active   │     Passive  │
                     ▼          │              ▼
         ┌───────────────────┐  │  ┌───────────────────┐
         │  PRIMARY REGION   │  │  │ SECONDARY REGION   │
         │  (us-east-1)      │  │  │ (us-west-2)        │
         │                   │  │  │                     │
         │  ┌─────────────┐  │  │  │  ┌─────────────┐   │
         │  │     ALB     │  │  │  │  │     ALB     │   │
         │  └──────┬──────┘  │  │  │  └──────┬──────┘   │
         │         │         │  │  │         │          │
         │  ┌──────┴──────┐  │  │  │  ┌──────┴──────┐   │
         │  │  EC2 / ASG  │  │  │  │  │  EC2 / ASG  │   │
         │  │(Full Scale) │  │  │  │  │(Scaled Down)│   │
         │  └──────┬──────┘  │  │  │  └──────┬──────┘   │
         │         │         │  │  │         │          │
         │  ┌──────┴──────┐  │  │  │  ┌──────┴──────┐   │
         │  │Aurora Primary│──┼──┼──┼─►│Aurora Read  │   │
         │  │  (Writer)   │  │  │  │  │  Replica    │   │
         │  └─────────────┘  │  │  │  └─────────────┘   │
         │                   │  │  │                     │
         │  ┌─────────────┐  │  │  │  ┌─────────────┐   │
         │  │  S3 Bucket  │──┼──┼──┼─►│  S3 Bucket  │   │
         │  │  (Primary)  │ CRR │  │  │  (Replica)  │   │
         │  └─────────────┘  │  │  │  └─────────────┘   │
         └───────────────────┘  │  └─────────────────────┘
                                │
                          Health Checks
```

**Key points:**
- Route 53 failover routing with health checks
- Primary region runs at full capacity
- Secondary region runs at reduced capacity (warm standby)
- Aurora Global Database: <1 sec replication, promote replica in DR
- S3 CRR for object replication
- RPO: seconds–minutes | RTO: minutes

---

## 4. Hybrid Cloud with Direct Connect

```
   ┌───────────────────────────────────────────────────────────────┐
   │                      ON-PREMISES DATA CENTER                  │
   │                                                               │
   │   ┌──────────┐   ┌──────────┐   ┌──────────────────────┐    │
   │   │ App      │   │ Database │   │ Customer Router      │    │
   │   │ Servers  │   │ Servers  │   │ (BGP)                │    │
   │   └──────────┘   └──────────┘   └──────────┬───────────┘    │
   │                                             │                 │
   └─────────────────────────────────────────────┼─────────────────┘
                                                 │
                                    ┌────────────┴────────────┐
                                    │  Direct Connect         │
                                    │  Location (DX)          │
                                    │                         │
                                    │  ┌───────────────────┐  │
                                    │  │ AWS Direct Connect │  │
                                    │  │ Router (1/10 Gbps) │  │
                                    │  └─────────┬─────────┘  │
                                    └────────────┼────────────┘
                                                 │
                                    Private VIF / Transit VIF
                                                 │
                              ┌──────────────────┴──────────────────┐
                              │          Transit Gateway            │
                              │         (Hub & Spoke)               │
                              └───┬──────────┬──────────┬───────────┘
                                  │          │          │
                    ┌─────────────┴┐  ┌──────┴────────┐ ┌┴─────────────┐
                    │  VPC - Shared │  │  VPC - Prod   │ │  VPC - Dev   │
                    │  Services     │  │               │ │              │
                    │  10.1.0.0/16  │  │  10.2.0.0/16  │ │ 10.3.0.0/16 │
                    │               │  │               │ │              │
                    │ ┌───────────┐ │  │ ┌───────────┐ │ │┌───────────┐│
                    │ │ Active    │ │  │ │ EC2 / ECS │ │ ││ EC2 / ECS ││
                    │ │ Directory │ │  │ │ Workloads │ │ ││ Workloads ││
                    │ └───────────┘ │  │ └───────────┘ │ │└───────────┘│
                    │ ┌───────────┐ │  │ ┌───────────┐ │ │┌───────────┐│
                    │ │ DNS       │ │  │ │ RDS/Aurora│ │ ││ RDS/Aurora││
                    │ │ Resolver  │ │  │ │ Databases │ │ ││ Databases ││
                    │ └───────────┘ │  │ └───────────┘ │ │└───────────┘│
                    └───────────────┘  └───────────────┘ └─────────────┘
```

**Key points:**
- Direct Connect provides dedicated, consistent network connection
- Transit Gateway acts as central hub connecting all VPCs and on-premises
- Transit VIF connects DX to Transit Gateway
- Shared Services VPC for DNS, AD, etc.
- DX + VPN for encrypted backup (IPSec over DX public VIF)

---

## 5. Data Lake Architecture

```
              DATA SOURCES                    INGEST                STORE
         ┌───────────────┐
         │  IoT Devices  │──┐
         └───────────────┘  │
         ┌───────────────┐  │    ┌──────────────┐       ┌──────────────────┐
         │  Application  │──┼───►│   Kinesis     │──────►│                  │
         │  Logs         │  │    │ Data Firehose │       │   S3 Data Lake   │
         └───────────────┘  │    └──────────────┘       │                  │
         ┌───────────────┐  │                            │  ┌────────────┐ │
         │  Databases    │──┘    ┌──────────────┐       │  │  Raw Zone  │ │
         └───────────────┘  ┌───►│     DMS      │──────►│  │ (Landing)  │ │
         ┌───────────────┐  │    └──────────────┘       │  ├────────────┤ │
         │  On-Premises  │──┘                            │  │ Processed  │ │
         │  Databases    │       ┌──────────────┐       │  │   Zone     │ │
         └───────────────┘       │  S3 Transfer │       │  ├────────────┤ │
         ┌───────────────┐       │  Acceleration│──────►│  │ Curated    │ │
         │  Files/CSV    │──────►│  / Direct    │       │  │   Zone     │ │
         └───────────────┘       └──────────────┘       │  └────────────┘ │
                                                         └────────┬───────┘
                                                                  │
                             CATALOG & TRANSFORM                  │
                                                                  │
                           ┌──────────────────┐                   │
                           │   AWS Glue       │◄──────────────────┘
                           │  (ETL + Catalog) │
                           │                  │
                           │ ┌──────────────┐ │
                           │ │ Glue Crawler │ │
                           │ │ (Schema Disc)│ │
                           │ ├──────────────┤ │
                           │ │ Glue ETL Job │ │
                           │ │ (Transform)  │ │
                           │ ├──────────────┤ │
                           │ │ Glue Data    │ │
                           │ │ Catalog      │ │
                           │ └──────────────┘ │
                           └────────┬─────────┘
                                    │
                    QUERY & ANALYZE  │
                    ┌────────────────┼─────────────────┐
                    │                │                  │
              ┌─────┴──────┐  ┌─────┴──────┐  ┌───────┴──────┐
              │   Athena   │  │  Redshift  │  │  QuickSight  │
              │ (SQL on S3)│  │  Spectrum   │  │  (Dashboard) │
              │            │  │ (Warehouse) │  │              │
              └────────────┘  └────────────┘  └──────────────┘
```

**Key points:**
- S3 is the central data lake (raw → processed → curated zones)
- Kinesis Data Firehose for streaming ingestion (near real-time)
- DMS for database migration/replication
- AWS Glue for ETL, schema discovery, and Data Catalog
- Athena for serverless SQL queries directly on S3
- Redshift Spectrum for complex analytics (external tables on S3)
- QuickSight for BI dashboards and visualization

---

## 6. Event-Driven Architecture (SNS Fan-Out)

```
                    ┌──────────────┐
                    │   Producer   │
                    │  (Order API) │
                    └──────┬───────┘
                           │ Publish
                           ▼
                    ┌──────────────┐
                    │   SNS Topic  │
                    │ (OrderEvents)│
                    └──┬───┬───┬───┘
                       │   │   │
          ┌────────────┘   │   └────────────┐
          │                │                │
          ▼                ▼                ▼
   ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
   │  SQS Queue   │ │  SQS Queue   │ │  SQS Queue   │
   │ (Fulfillment)│ │  (Billing)   │ │(Notification)│
   └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
          │                │                │
          ▼                ▼                ▼
   ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
   │   Lambda     │ │   Lambda     │ │   Lambda     │
   │ (Process     │ │ (Generate    │ │ (Send Email  │
   │  Order)      │ │  Invoice)    │ │  via SES)    │
   └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
          │                │                │
          ▼                ▼                ▼
   ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
   │  DynamoDB    │ │     S3       │ │     SES      │
   │ (Order Table)│ │  (Invoices)  │ │  (Emails)    │
   └──────────────┘ └──────────────┘ └──────────────┘
```

**Key points:**
- SNS topic fans out to multiple SQS queues
- Each SQS queue acts as a buffer for its consumer
- SQS provides fault tolerance (retry, DLQ)
- Lambda consumers process independently
- If one consumer fails, others are unaffected
- SQS DLQ captures failed messages for investigation

---

## 7. Microservices on ECS/Fargate

```
  ┌──────────────┐    ┌───────────────────────────────────────────────────────┐
  │  CodeCommit  │    │                    VPC                                │
  │  (Source)    │    │                                                       │
  └──────┬───────┘    │  ┌── Public Subnets ─────────────────────────────┐   │
         │            │  │                                                │   │
         ▼            │  │           ┌──────────────┐                    │   │
  ┌──────────────┐    │  │           │     ALB      │                    │   │
  │ CodePipeline │    │  │           │  (Internet-  │                    │   │
  │              │    │  │           │   facing)    │                    │   │
  │ ┌──────────┐ │    │  │           └──────┬───────┘                    │   │
  │ │CodeBuild │ │    │  │                  │                            │   │
  │ │(Build +  │ │    │  └──────────────────┼────────────────────────────┘   │
  │ │ Test)    │ │    │                     │                                │
  │ └────┬─────┘ │    │  ┌── Private Subnets ───────────────────────────┐   │
  │      │       │    │  │                  │                            │   │
  │      ▼       │    │  │    ┌─────────────┼─────────────┐             │   │
  │ ┌──────────┐ │    │  │    │             │             │             │   │
  │ │   ECR    │ │    │  │    ▼             ▼             ▼             │   │
  │ │(Registry)│ │    │  │ ┌─────────┐ ┌─────────┐ ┌─────────┐        │   │
  │ └──────────┘ │    │  │ │ECS Svc A│ │ECS Svc B│ │ECS Svc C│        │   │
  │      │       │    │  │ │(Fargate)│ │(Fargate)│ │(Fargate)│        │   │
  │      ▼       │    │  │ │  User   │ │  Order  │ │ Payment │        │   │
  │ ┌──────────┐ │    │  │ │ Service │ │ Service │ │ Service │        │   │
  │ │CodeDeploy│ │    │  │ └────┬────┘ └────┬────┘ └────┬────┘        │   │
  │ │(Deploy)  │ │    │  │      │           │           │              │   │
  │ └──────────┘ │    │  └──────┼───────────┼───────────┼──────────────┘   │
  └──────────────┘    │         │           │           │                   │
                      │  ┌── Private Subnets (Data) ────────────────────┐  │
                      │  │      │           │           │                │  │
                      │  │      ▼           ▼           ▼                │  │
                      │  │ ┌─────────┐ ┌─────────┐ ┌─────────┐         │  │
                      │  │ │DynamoDB │ │   RDS   │ │DynamoDB │         │  │
                      │  │ │ (Users) │ │ (Orders)│ │(Payments│         │  │
                      │  │ └─────────┘ └─────────┘ └─────────┘         │  │
                      │  └──────────────────────────────────────────────┘  │
                      └────────────────────────────────────────────────────┘
```

**Key points:**
- Each microservice is an independent ECS Service on Fargate
- ALB routes to different services via path-based routing rules
- ECR stores Docker images
- CodePipeline automates CI/CD (Source → Build → Deploy)
- Each service has its own database (database-per-service pattern)
- Services communicate via ALB or service discovery (Cloud Map)

---

## 8. Multi-Account Organization

```
                    ┌──────────────────────────────┐
                    │    AWS Organizations Root     │
                    └──────────────┬───────────────┘
                                   │
                    ┌──────────────┴───────────────┐
                    │     Management Account       │
                    │                              │
                    │  • AWS Organizations          │
                    │  • Billing / Cost Management  │
                    │  • SCPs (Service Control)     │
                    │  • CloudFormation StackSets   │
                    │  • IAM Identity Center (SSO)  │
                    └──────────────┬───────────────┘
                                   │
              ┌────────────────────┼────────────────────┐
              │                    │                    │
     ┌────────┴────────┐  ┌───────┴────────┐  ┌───────┴────────┐
     │   Security OU   │  │   Sandbox OU   │  │  Workloads OU  │
     └────────┬────────┘  └───────┬────────┘  └───────┬────────┘
              │                    │              ┌────┴─────┐
     ┌────────┼────────┐          │              │          │
     │        │        │          │              │          │
┌────┴───┐┌───┴────┐   │    ┌────┴───┐    ┌────┴───┐ ┌────┴───┐
│Security││Log     │   │    │  Dev   │    │Staging │ │  Prod  │
│Account ││Account │   │    │Account │    │Account │ │Account │
│        ││        │   │    │        │    │        │ │        │
│•Guard- ││•Cloud- │   │    │•Dev    │    │•Test   │ │•Prod   │
│ Duty   ││ Trail  │   │    │ VPCs   │    │ VPCs   │ │ VPCs   │
│ (Deleg)││ (Org   │   │    │•Dev    │    │•Test   │ │•Prod   │
│•Sec Hub││  Trail)│   │    │ Envs   │    │ Envs   │ │ Envs   │
│•Config ││•S3 Log │   │    │        │    │        │ │•HA/DR  │
│ (Aggr) ││ Bucket │   │    │        │    │        │ │        │
│•Detect-││•Config │   │    │        │    │        │ │        │
│ ive    ││ Logs   │   │    │        │    │        │ │        │
└────────┘└────────┘   │    └────────┘    └────────┘ └────────┘
                       │
                 ┌─────┴──────┐
                 │  Network   │
                 │  Account   │
                 │            │
                 │•Transit GW │
                 │•Direct Conn│
                 │•VPN        │
                 │•DNS (R53)  │
                 └────────────┘
```

**Key points:**
- Management Account: billing, SCPs, organization management only (no workloads)
- Security Account: delegated admin for GuardDuty, Security Hub, Config
- Log Account: centralized CloudTrail, Config, VPC Flow Logs
- Network Account: shared networking (Transit Gateway, Direct Connect)
- Workload OUs: separate Dev / Staging / Prod environments
- SCPs restrict actions at the OU level (e.g., deny region usage, deny root account usage)
- CloudFormation StackSets deploy baseline resources across all accounts

---

## 9. VPC with Public and Private Subnets (2 AZs)

```
┌────────────────────────────────────────────────────────────────────────────┐
│                          VPC: 10.0.0.0/16                                  │
│                                                                            │
│    ┌────────────┐                                                          │
│    │    IGW     │ ◄── Internet Gateway (1 per VPC)                        │
│    └─────┬──────┘                                                          │
│          │                                                                 │
│  ┌───────┴──────────────────────────┬──────────────────────────────────┐   │
│  │                                  │                                  │   │
│  │      Availability Zone A         │      Availability Zone B         │   │
│  │                                  │                                  │   │
│  │  ┌────────────────────────────┐  │  ┌────────────────────────────┐  │   │
│  │  │  Public Subnet A           │  │  │  Public Subnet B           │  │   │
│  │  │  10.0.1.0/24               │  │  │  10.0.2.0/24               │  │   │
│  │  │                            │  │  │                            │  │   │
│  │  │  ┌──────────────────────┐  │  │  │  ┌──────────────────────┐  │  │   │
│  │  │  │   NAT Gateway A     │  │  │  │  │   NAT Gateway B     │  │  │   │
│  │  │  │   (Elastic IP)      │  │  │  │  │   (Elastic IP)      │  │  │   │
│  │  │  └──────────┬───────────┘  │  │  │  └──────────┬───────────┘  │  │   │
│  │  │             │              │  │  │             │              │  │   │
│  │  │  Route Table (Public):     │  │  │  Route Table (Public):     │  │   │
│  │  │  10.0.0.0/16 → local      │  │  │  10.0.0.0/16 → local      │  │   │
│  │  │  0.0.0.0/0   → IGW        │  │  │  0.0.0.0/0   → IGW        │  │   │
│  │  └────────────────────────────┘  │  └────────────────────────────┘  │   │
│  │                                  │                                  │   │
│  │  ┌────────────────────────────┐  │  ┌────────────────────────────┐  │   │
│  │  │  Private Subnet A          │  │  │  Private Subnet B          │  │   │
│  │  │  10.0.3.0/24               │  │  │  10.0.4.0/24               │  │   │
│  │  │                            │  │  │                            │  │   │
│  │  │  ┌──────────┐ ┌─────────┐  │  │  │  ┌──────────┐ ┌─────────┐  │  │   │
│  │  │  │   EC2    │ │   EC2   │  │  │  │  │   EC2    │ │   EC2   │  │  │   │
│  │  │  │ Instance │ │ Instance│  │  │  │  │ Instance │ │ Instance│  │  │   │
│  │  │  └──────────┘ └─────────┘  │  │  │  └──────────┘ └─────────┘  │  │   │
│  │  │                            │  │  │                            │  │   │
│  │  │  Route Table (Private A):  │  │  │  Route Table (Private B):  │  │   │
│  │  │  10.0.0.0/16 → local      │  │  │  10.0.0.0/16 → local      │  │   │
│  │  │  0.0.0.0/0   → NAT-GW-A   │  │  │  0.0.0.0/0   → NAT-GW-B   │  │   │
│  │  └────────────────────────────┘  │  └────────────────────────────┘  │   │
│  │                                  │                                  │   │
│  └──────────────────────────────────┴──────────────────────────────────┘   │
│                                                                            │
│    NACL: Subnet-level firewall (stateless)                                │
│    Security Groups: Instance-level firewall (stateful)                    │
└────────────────────────────────────────────────────────────────────────────┘
```

**Key points:**
- 1 IGW per VPC (provides internet access)
- NAT Gateway in each AZ for high availability
- Public subnets route 0.0.0.0/0 → IGW
- Private subnets route 0.0.0.0/0 → NAT Gateway (same AZ)
- Each subnet has its own route table
- AWS reserves 5 IPs per subnet (.0, .1, .2, .3, .255)

---

## 10. S3 Cross-Region Replication with CloudFront

```
                         ┌──────────────┐
                         │    Users     │
                         │  (Global)    │
                         └──────┬───────┘
                                │
                         ┌──────┴───────┐
                         │  CloudFront  │
                         │ Distribution │
                         │              │
                         │  ┌────────┐  │
                         │  │ Edge   │  │
                         │  │Locations│  │
                         │  │(400+)  │  │
                         │  └───┬────┘  │
                         └──────┼───────┘
                                │
              ┌─────────────────┼─────────────────┐
              │  Origin Group   │                  │
              │  (Failover)     │                  │
              ▼                 │                  ▼
   ┌────────────────────┐      │       ┌────────────────────┐
   │   PRIMARY REGION   │      │       │  SECONDARY REGION   │
   │   (us-east-1)      │      │       │  (eu-west-1)        │
   │                     │      │       │                     │
   │  ┌──────────────┐  │      │       │  ┌──────────────┐   │
   │  │  S3 Bucket   │  │      │       │  │  S3 Bucket   │   │
   │  │  (Source)    │──┼──CRR─┼──────►│  │  (Replica)   │   │
   │  │              │  │      │       │  │              │   │
   │  │ • Versioning │  │      │       │  │ • Versioning │   │
   │  │   Enabled    │  │      │       │  │   Enabled    │   │
   │  │ • OAC Policy │  │      │       │  │ • OAC Policy │   │
   │  └──────────────┘  │      │       │  └──────────────┘   │
   │                     │      │       │                     │
   └────────────────────┘      │       └─────────────────────┘
                                │
                         ┌──────┴───────┐
                         │    OAC       │
                         │(Origin Access│
                         │  Control)    │
                         └──────────────┘
```

**Key points:**
- CloudFront caches content at 400+ edge locations for low-latency access
- S3 CRR replicates objects to a secondary region (versioning required on both)
- CloudFront Origin Group enables automatic failover to replica bucket
- Origin Access Control (OAC) restricts S3 access to CloudFront only
- Users always hit CloudFront (not S3 directly)
- If primary S3 returns error, CloudFront falls back to secondary bucket
- SSL/TLS via ACM certificate (must be in us-east-1 for CloudFront)
